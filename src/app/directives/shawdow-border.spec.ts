import { ShawdowBorder } from './shawdow-border';

describe('ShawdowBorder', () => {
  it('should create an instance', () => {
    const directive = new ShawdowBorder();
    expect(directive).toBeTruthy();
  });
});
